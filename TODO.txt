3) send login/password encrypted
4) limit max size of pod to be uploaded
4) create indexes for the mongo collections
4) reset password feature + my account page 
5) user page ie: /user/tcolar would show user full name, website and maybe gravatar :)
4) More unit tests !
5) browse/search sorting & pagination (jquery ? )
5) pod depends count ... might need a background job for that to update the count every hours or something like that
5) allow user to modify some of the pod meta after it's published ? maybe ... but not version or license for example.
5) Make menus more interactive, "highlight" the active one
